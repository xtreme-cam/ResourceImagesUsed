Determine what images are referenced inside your code.

Basic usage:

./images_used [-vc] [INPUT DIRECTORY]

-v      Verbose Comments
-c      Exclude resource references in comments
-s      Generate script to unused resource files

Tests:

Run iOS tests:

*** requires test-unit gem ***

ruby ios_tests.rb
